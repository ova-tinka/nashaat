import 'package:flutter/material.dart';

class AppColors {
  // Ink — blacks
  static const Color ink = Color(0xFF0A0A0A);
  static const Color inkSoft = Color(0xFF1A1A1A);
  static const Color inkMuted = Color(0xFF5A5A5A);

  // Paper — whites / greys
  static const Color paper = Color(0xFFFFFFFF);
  static const Color paperAlt = Color(0xFFF5F5F5);
  static const Color paperBorder = Color(0xFFE5E5E5);

  // Acid — green accent
  static const Color acid = Color(0xFFB6FF3C);
  static const Color acidPressed = Color(0xFF7ACC1A);
  static const Color acidMuted = Color(0xFFD9FFAA);

  // Signal — yellow accent
  static const Color signal = Color(0xFFFFD400);
  static const Color signalPressed = Color(0xFFE0BB00);
  static const Color signalMuted = Color(0xFFFFF0A0);

  // Semantic
  static const Color error = Color(0xFFCC2200);
  static const Color errorMuted = Color(0xFFFFE8E5);
  static const Color success = acid;
}
