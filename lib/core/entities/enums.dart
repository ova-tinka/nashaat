enum ItemType { app, website }

enum FriendshipStatus { pending, accepted, rejected }

enum SubscriptionTier { free, vip }

enum UserStatus { active, verified, onboarded, inactive, deleted, suspended }

enum WorkoutSource { manual, aiGenerated }

enum RuleStatus { active, inactive, archived }

enum NotificationStatus { unread, read, archived }

enum MediaType { image, video, document }

enum ExerciseMeasurement { repsWeight, timeDistance, timeOnly, repsOnly }

enum TransactionType { earned, spent, penalty, manualAdjustment }
